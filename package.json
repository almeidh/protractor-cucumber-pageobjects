{
  "name": "protractor-cucumber-pageobjects",
  "description": "protractor with cucumber using page object model with reporting",
  "devDependencies": {
    "protractor": "^5.4.2",
    "cucumber": "^1.3.3",
    "protractor-cucumber-framework": "^3.1.2",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1"
  },
  "scripts": {
    "prestart": "webdriver-manager update",
    "server": "webdriver-manager start",
    "test": "protractor conf.js"
  }
}
